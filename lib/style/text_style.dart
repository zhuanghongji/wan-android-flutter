import 'package:flutter/material.dart';

/// 文本样式
class TextStyles {

  /// 作者
  static author() => TextStyle (
    fontSize: 18,
  );

  /// 作者
  static publishTime() => TextStyle (
    fontSize: 16,
  );

  /// 作者
  static title() => TextStyle (
    fontSize: 20, 
    fontWeight: FontWeight.bold
  );

  /// 作者
  static desc() => TextStyle (
    fontSize: 16, 
  );

  /// 作者
  static superChapterName() => TextStyle (
    fontSize: 14, 
  );

}